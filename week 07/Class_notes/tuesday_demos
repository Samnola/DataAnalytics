## Splitting Arrays
import numpy as np
arr = np.array([1, 2, 3, 4, 5, 6])
arr.ndim

print(np.array_split(arr, 2))

print(np.array_split(arr, 3))

print(np.array_split(arr, 4)) #array split for when youre not doing even split

#split helper functions: hsplit(), vsplit(), dsplit()

print(np.hsplit(arr, 2))

#print(np.hsplit(arr, 4))
# will throw value error hsplit req split to be divided equally

arr